<div class='container' style='padding-top: 10px;'>
  <div class='row'>
    <div class='col-sm-2'><%= link_to post.user.username, user_path(post.user) %> posted about the weather in <%= link_to_if logged_in?, name_lookup(post.location.city_id), location_path(post.location) %>: </div>
    <div class='col-sm-10 panel-body well'>
      <p><em><%= post.body %></em></p>
      <p>on <%= display_datetime(post.created_at) %></p>
    </div>
  </div>
</div>